#ifndef TEST_CREATE_PATCHES_H_
#define TEST_CREATE_PATCHES_H_

#include "test_helper.h" // Provides run_test_patch signature and helper declarations

// Declare the function that runs all tests for create_patches
// It takes pointers to total and passed counts to update them.
void run_all_create_patches_tests(int* total, int* passed);

#endif // TEST_CREATE_PATCHES_H_